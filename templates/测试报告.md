# 测试报告：{需求标题}

> 测试执行完成后，严格按此模板格式输出测试报告。
> 本报告由 software-dev-ob skill 按需求 {REQ-XXX} 生成。

| 文档信息 | 内容 |
|----------|------|
| 关联需求 | [[{REQ-XXX}-{slug}]] |
| 关联用例 | [[{REQ-XXX}-{slug}-用例]] |
| 测试版本 | v1.0 |
| 测试环境 | {开发/测试/预发布} |
| 测试周期 | {{date}} ~ {{date}} |
| 测试人员 | 质心 |

---

## 一、测试概要

| 指标 | 数值 |
|------|------|
| 用例总数 | {N} |
| 通过 | {N} |
| 失败 | {N} |
| 阻塞 | {N}（无法执行，等待依赖） |
| 跳过 | {N}（不适用的场景） |
| **通过率** | **{百分比}%** |

---

## 二、测试环境

> ⚠️ 接口文档里的路径前缀（如 `/api/v1`）只是前缀，实际请求 = `{测试地址} + {接口文档路径}`，拼接时去重斜杠。

| 项目 | 说明 |
|------|------|
| 测试环境 | {环境地址/名称} |
| 测试地址 | {测试环境地址} |
| 数据库 | {数据库版本/库名} |
| 分支/版本 | {Git分支或版本号} |
| 部署时间 | {最近部署时间} |

---

## 三、测试结果明细

### 3.1 单接口测试

| 用例编号 | 测试场景 | 优先级 | 入参 | 预期出参 | 实际出参 | 结果 | 备注 |
|----------|----------|--------|------|----------|----------|------|------|
| API-{reqNum}-001 | {场景描述} | P0 | `{请求参数}` | `code=200, data={...}` | `code=200, data={...}` | ✅ 通过 | - |
| API-{reqNum}-002 | {场景描述} | P1 | `{请求参数}` | `code=-10, msg="参数校验错误"` | `code=200, msg="success"` | ❌ 失败 | 见 Bug #001 |
| API-{reqNum}-003 | {场景描述} | P2 | `{请求参数}` | - | - | ⏸️ 跳过 | {跳过原因} |

### 3.2 接口组合测试

| 用例编号 | 测试场景 | 优先级 | 步骤 | 入参 | 预期出参 | 实际出参 | 结果 | 备注 |
|----------|----------|--------|------|------|----------|----------|------|------|
| COMBO-{reqNum}-001 | {场景描述} | P0 | 1. 接口A创建<br>2. 接口B查询 | `{接口A入参}`<br>`{接口B入参}` | `{预期出参}` | `{实际出参}` | ✅ 通过 | - |
| COMBO-{reqNum}-002 | {场景描述} | P1 | 1. 接口A创建<br>2. 接口B修改<br>3. 接口C查询 | `{各接口入参}` | `{预期出参}` | `{实际出参}` | ❌ 失败 | 见 Bug #002 |

### 3.3 数据一致性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 写入一致性 | ✅/❌ | {说明} |
| 关联数据 | ✅/❌ | {说明} |
| 状态流转 | ✅/❌ | {说明} |
| 幂等性 | ✅/❌ | {说明} |
| 删除重写 | ✅/❌ | {说明} |

---

## 四、Bug 清单

| Bug编号 | 关联用例 | 严重程度 | Bug描述 | 入参 | 预期出参 | 实际出参 | 状态 |
|---------|----------|----------|---------|------|----------|----------|------|
| BUG-001 | API-{reqNum}-002 | 严重 | {简要描述} | `{请求参数}` | `{预期响应}` | `{实际响应}` | 待修复 |
| BUG-002 | COMBO-{reqNum}-002 | 一般 | {简要描述} | `{请求参数}` | `{预期响应}` | `{实际响应}` | 待修复 |

### Bug 严重程度定义

| 级别 | 说明 | 举例 |
|------|------|------|
| 致命 | 系统崩溃、数据丢失、核心接口不可用 | 写入成功但数据丢失 |
| 严重 | 主要接口异常、数据错误 | 业务校验缺失、数据写入错误 |
| 一般 | 次要接口异常、边界场景未处理 | 非必填字段校验缺失 |
| 轻微 | 响应格式问题、不影响功能的优化项 | 错误提示文案不友好 |

---

## 五、风险评估

| 风险项 | 风险等级 | 说明 | 影响范围 | 建议措施 |
|--------|----------|------|----------|----------|
| {风险描述} | 高/中/低 | {详细说明} | {影响的接口/模块} | {建议} |

---

## 六、结论

| 结论项 | 判定 |
|--------|------|
| 是否可上线 | ✅ 可上线 / ❌ 不可上线 / ⚠️ 有条件上线 |
| 阻塞项 | {列出阻塞上线的 Bug 或风险} |
| 遗留问题 | {列出已知但不阻塞上线的问题} |
| 建议 | {测试角度的建议} |

---

## 七、附录

- 测试用例文件：`{{kbDir}}/tests/{REQ-XXX}-{slug}-用例.md`
- 相关需求：`{{kbDir}}/00-需求/{REQ-XXX}-{slug}.md`
